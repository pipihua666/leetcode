const currentQueue = (urls, maxNum) => {
	return new Promise((resolve, reject) => {
		if (urls.length === 0) {
			resolve([]);
			return;
		}

		const results = [];
		let index = 0;
		let count = 0;

		const request = () => { 
			const i = index;
			
		}
	})

}